# Methods to Practice

Counter()
.items()
.keys()
.values()
.most_common()

sorted(vals, key = vals.get, reverse = True)
groupby()

bisect_left(vals)

.isalnum()
.isalpha()

## Math

```python
import math

n = 32
print(math.log(n, 2))   # 5.0
```

.bit_length()
The number of bits required to represent n in binary, excluding the sign bit

divmod()
