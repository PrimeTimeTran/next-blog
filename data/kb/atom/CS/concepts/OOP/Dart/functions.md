# functions

- typed parameters
  `dart
int sum(int a, int b) {
	return a + b;
}`
- untyped
  ````dart
  	int sum(a, b) {
  		return a + b;
  	}```
  ````
- named parameters
  ````dart
  	double divide({numerator: a, denominator: b}) {
  		return a / b;
  	}```
  ````
